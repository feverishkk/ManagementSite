@page "/itemlist/totalAcc"
@attribute [Authorize (Roles = "SuperAdministrator, ServerAdministrator, Basic")]

<MudTable Items="@TotalAcc">
    <ToolBarContent>
        <MudText Typo="Typo.h6">Accessories List</MudText>
    </ToolBarContent>
    <HeaderContent>
        <MudTh>No</MudTh>
        <MudTh>Image</MudTh>
        <MudTh>Name</MudTh>
        <MudTh>AC</MudTh>
        <MudTh>Class</MudTh>
        <MudTh>Description</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="No">@context.TotalAccId</MudTd>
        <MudTd DataLabel="Image">
            <img src="@context.Image" style="height:100px; width:100px" />
        </MudTd>
        <MudTd DataLabel="Name">@context.Name</MudTd>
        <MudTd DataLabel="AC">@context.AC</MudTd>
        <MudTd DataLabel="Class">@context.Class</MudTd>
        <MudTd DataLabel="Description">@context.Description</MudTd>
    </RowTemplate>
    <PagerContent>
    <MudTablePager PageSizeOptions="pageSizeOptions"></MudTablePager>
    </PagerContent>
</MudTable>


@code {
    private int[] pageSizeOptions = new int[] { 5, 10, 15, 50, 100 };

    private TotalAccessories totalAccs = new();
    private IEnumerable<TotalAccessories> TotalAcc { get; set; } = new List<TotalAccessories>();

    protected override async Task OnInitializedAsync()
    {
        await LoadData();
        await base.OnInitializedAsync();
    }

    private async Task LoadData()
    {
        TotalAcc = await _getItemHttpService.GetAllAcc();
    }

}