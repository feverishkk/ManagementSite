<MudDialog>
    <DialogContent>
        <MudForm Model="@itemId">
            <MudTextField Label="OneHandSwordId" Value="itemId" ReadOnly="true" />
            <MudTextField Label="OneHandSwordName" Value="itemName" ReadOnly="true" />
            <img src="@itemImage" style="width:200px; height:100px;" />
            <MudText Typo="Typo.h4" Class="mt-2"> Are you sure? </MudText>
        </MudForm>

    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Primary" OnClick="Submit">Ok</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    [Parameter] public int itemId{ get; set; }
    [Parameter] public string itemName { get; set; }
    [Parameter] public string itemImage { get; set; }

    [Parameter] public OneHandSword model { get; set; } = new();

    void Submit()
    {
        model.OneHandSwordId = itemId;
        MudDialog.Close(DialogResult.Ok<OneHandSword>(model));
    }

    void Cancel()
    {
        MudDialog.Cancel();
    }


}