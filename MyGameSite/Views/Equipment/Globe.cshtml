@using CommonDatabase.Models.Equipment
@model IEnumerable<Globe>

<div style="height:300px">
    <h4>장갑</h4>
    <hr />

    <p class="text-left ">
        캐릭터의 손 부위에 장착하는 아이템으로, AC보다는 STR, DEX 같은 스탯이나 활 명중 등의 옵션이 주를 이룬다.
    </p>
</div>

<h4> Globe </h4>
<hr />

<table class="table table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>AC</th>
            <th>Class</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>@item.GlobeId</th>
                <th class="text-center table-active">
                    <img src="@item.Image" style="width:65px; height:65px;" /> 
                    <br /> @item.Name 
                </th>
                <th>@item.Class</th>
                <th>@item.AC</th>
                <th>@item.Description</th>
            </tr>
        }
    </tbody>
</table>